set nu "set line number

set shiftwidth=4
set softtabstop=4
set tabstop=4
set expandtab

set smartindent " autoindent, cindent, indentexpr

set cursorline " highlight current cursor line
set mouse=a " support mouse

set ignorecase "ignore case when search

colorscheme desert " set color cheme

imap jj <ESC> "map jj into ESC
